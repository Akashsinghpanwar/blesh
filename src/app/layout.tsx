import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CrystalStim - Smart Emotional Regulation Wearable",
  description: "Real-time biosensor simulation demonstrating passive emotional regulation for neurodivergent individuals through sensory anchoring technology.",
  keywords: ["emotional regulation", "neurodivergent", "wearable", "biosensor", "HRV", "stress management"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
